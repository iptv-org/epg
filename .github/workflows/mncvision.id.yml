name: mncvision.id
on:
  schedule:
    - cron: '00 14 * * *'
  workflow_dispatch:
  workflow_run:
    workflows: [_trigger]
    types:
      - completed
jobs:
  load:
    uses: ./.github/workflows/_load.yml
    with:
      site: ${{github.workflow}}
